<!DOCTYPE html>
<html dir="ltr">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Admin Portal | Login</title>
    
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.6.2/dist/css/bootstrap.min.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/MaterialDesign-Webfont/7.2.96/css/materialdesignicons.min.css">
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600;800&display=swap" rel="stylesheet">
    
    <style>
        body, html { height: 100%; margin: 0; font-family: 'Inter', sans-serif; }
        
        .auth-wrapper { 
            background: linear-gradient(135deg, #0f172a 0%, #334155 100%); 
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            padding: 20px;
        }

        .auth-box { 
            background: #ffffffcf; 
            padding: 45px 35px;
            border-radius: 24px;
            box-shadow: 0 25px 50px -12px rgba(0,0,0,0.25);
            width: 100%;
            max-width: 420px;
        }

        .logo-text { 
            font-size: 26px; 
            font-weight: 800; 
            color: #1a1a1a; 
            letter-spacing: -0.5px;
            margin-bottom: 5px;
        }

        .form-group label {
            color: #4b5563;
            font-size: 13px;
            margin-bottom: 8px;
            display: block;
        }

        .input-group-text {
            background: transparent;
            border-right: none;
            border-radius: 12px 0 0 12px;
            color: #9ca3af;
        }

        .form-control {
            border-radius: 12px;
            padding: 14px 15px;
            height: auto;
            border: 1px solid #e5e7eb;
            font-size: 15px;
            transition: all 0.2s;
        }

        .form-control:focus {
            border-color: #764ba2;
            box-shadow: 0 0 0 4px rgba(118, 75, 162, 0.1);
        }

        .btn-login {
            background: #764ba2;
            border: none;
            border-radius: 12px;
            padding: 14px;
            font-weight: 700;
            font-size: 16px;
            color: white;
            transition: 0.3s;
            margin-top: 10px;
        }

        .btn-login:hover { 
            background: #5a3a80; 
            color: white; 
            transform: translateY(-1px);
            box-shadow: 0 10px 15px -3px rgba(118, 75, 162, 0.4);
        }

        .forgot-link {
            color: #6b7280;
            font-size: 13px;
            font-weight: 500;
            transition: 0.2s;
        }

        .forgot-link:hover { color: #764ba2; text-decoration: none; }
    </style>
</head>
<body>

<div class="auth-wrapper">
    <div class="auth-box">
        
        <div id="loginform">
            <div class="text-center mb-5">
                <div class="logo-text">ADMIN PORTAL</div>
                <p class="text-muted small">Welcome back! Please enter your details.</p>
            </div>
            
            <form action="/login" method="post">
                <div class="form-group mb-4">
                    <label class="font-weight-bold">Email Address</label>
                    <input type="email" class="form-control" name="email" placeholder="admin@company.com" required>
                </div>
                
                <div class="form-group mb-3">
                    <label class="font-weight-bold">Password</label>
                    <input type="password" class="form-control" name="password" placeholder="••••••••" required>
                </div>

                <div class="text-right mb-4">
                    <button class="btn btn-link forgot-link p-0" id="to-recover" type="button">Forgot password?</button>
                </div>
                
                <button type="submit" class="btn btn-login btn-block">Sign In</button>
            </form>
        </div>

        <div id="recoverform" style="display: none;">
            <div class="text-center mb-5">
                <h4 class="font-weight-bold">Reset Password</h4>
                <p class="text-muted small">We'll send a recovery link to your email</p>
            </div>
            <form action="/verify-email" method="post">
                <div class="form-group mb-4">
                    <label class="font-weight-bold">Registered Email</label>
                    <input type="email" class="form-control" name="email" placeholder="Enter your email" required>
                </div>
                <button class="btn btn-login btn-block mb-3" type="submit">Send Reset Link</button>
                <div class="text-center">
                    <button class="btn btn-link forgot-link" id="to-login" type="button">
                        <i class="mdi mdi-arrow-left"></i> Back to Login
                    </button>
                </div>
            </form>
        </div>

    </div>
</div>

<script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
<script>
    $('#to-recover').click(function() {
        $("#loginform").fadeOut(200, function() {
            $("#recoverform").fadeIn();
        });
    });
    $('#to-login').click(function() {
        $("#recoverform").fadeOut(200, function() {
            $("#loginform").fadeIn();
        });
    });
</script>

</body>
</html>