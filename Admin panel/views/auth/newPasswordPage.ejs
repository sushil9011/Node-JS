<!DOCTYPE html>
<html dir="ltr">

<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>New Password | Admin Portal</title>

    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.6.2/dist/css/bootstrap.min.css">
    <link rel="stylesheet"
        href="https://cdnjs.cloudflare.com/ajax/libs/MaterialDesign-Webfont/7.2.96/css/materialdesignicons.min.css">
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600;700&display=swap" rel="stylesheet">

    <style>
        body,
        html {
            height: 100%;
            margin: 0;
            font-family: 'Inter', sans-serif;
            /* background: linear-gradient(135deg, #4b6cb7 0%, #182848 100%);  */
            background: linear-gradient(135deg, #0f172a 0%, #334155 100%);
            display: flex;
            align-items: center;
            justify-content: center;
        }

        .auth-box {
            background:  #3341557d;
            padding: 40px;
            border-radius: 24px;
            box-shadow: 0 20px 40px rgba(0, 0, 0, 0.3);
            width: 100%;
            max-width: 400px;
            text-align: center;
        }

        .profile-img-container {
            position: relative;
            display: inline-block;
            margin-bottom: 20px;
        }

        .profile-img {
            width: 85px;
            height: 85px;
            border-radius: 50%;
            object-fit: cover;
            border: 4px solid #f8f9fa;
            box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
        }

        .status-dot {
            height: 15px;
            width: 15px;
            background-color: #2ecc71;
            border-radius: 50%;
            display: block;
            position: absolute;
            bottom: 5px;
            right: 5px;
            border: 2px solid white;
        }

        .form-group label {
            text-align: left;
            display: block;
            color: #4b5563;
            font-size: 13px;
        }

        .form-control {
            border-radius: 12px;
            padding: 14px 15px;
            height: auto;
            border: 1px solid #e2e8f0;
            font-size: 15px;
        }

        .form-control:focus {
            border-color: #4b6cb7;
            box-shadow: 0 0 0 4px rgba(75, 108, 183, 0.1);
        }

        .btn-reset {
            background: #4b6cb7;
            border: none;
            border-radius: 12px;
            padding: 14px;
            font-weight: 700;
            color: white;
            width: 100%;
            transition: 0.3s;
            font-size: 16px;
        }

        .btn-reset:hover {
            background: #35508b;
            transform: translateY(-1px);
            box-shadow: 0 10px 15px rgba(0, 0, 0, 0.1);
        }

        .welcome-text {
            color: #f8f9fa;
            font-weight: 700;
            font-size: 1.25rem;
            margin-bottom: 0;
        }
    </style>
</head>

<body>
    <div class="auth-box">
        <div class="profile-img-container">
            <img src="/<%= admin.profile_image %>" class="profile-img">
            <span class="status-dot"></span>
        </div>

        <div class="mb-4">
            <h5 class="welcome-text">Hi, <%= admin.fname %>!</h5>
            <p class="text-muted small">Enter a secure new password for your account</p>
        </div>

        <form action="/change-new-password" method="post">
            <div class="form-group mb-3">
                <label class="font-weight-bold">New Password</label>
                <div class="input-group">
                    <input type="password" class="form-control" name="new_password" placeholder="••••••••" required>
                </div>
            </div>

            <div class="form-group mb-4">
                <label class="font-weight-bold">Confirm Password</label>
                <input type="password" class="form-control" name="conform_password" placeholder="••••••••" required>
            </div>

            <button type="submit" class="btn btn-reset shadow">
                Update Password
            </button>
        </form>

        <div class="mt-4">
            <a href="/" class="text-muted small" style="text-decoration: none;">
                <i class="mdi mdi-arrow-left"></i> Back to Login
            </a>
        </div>
    </div>
</body>

</html>