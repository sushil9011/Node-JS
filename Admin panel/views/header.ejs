<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Sushil Admin | Dashboard</title>
    
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">

    <style>
        :root {
            --sidebar-width: 260px;
            --primary-color: #214ca1b5;
            --dark-navy: #091a27;
            --bg-light: #f4f7f6;
        }

        body {
            font-family: 'Inter', sans-serif;
            background-color: var(--bg-light);
            margin: 0;
        }

        /* Sidebar */
        .sidebar {
            width: var(--sidebar-width);
            height: 100vh;
            background: var(--dark-navy);
            color: white;
            position: fixed;
            left: 0; top: 0;
            transition: 0.3s;
            z-index: 1050;
        }

        .main-wrapper {
            margin-left: var(--sidebar-width);
            min-height: 100vh;
            transition: 0.3s;
        }

        /* Navbar */
        .navbar {
            background: white;
            padding: 12px 30px;
            border-bottom: 1px solid #e9ecef;
        }

        .nav-link {
            color: #adb5bd;
            padding: 12px 20px;
            border-radius: 8px;
            margin: 4px 15px;
            transition: 0.2s;
            display: flex;
            align-items: center;
        }

        .nav-link i { width: 28px; font-size: 1.1rem; }

        .nav-link:hover, .nav-link.active {
            background: var(--primary-color);
            color: white !important;
        }

        /* Profile Dropdown Styling */
        .profile-pic {
            width: 38px; height: 38px;
            border-radius: 50%;
            object-fit: cover;
            border: 2px solid var(--primary-color);
            cursor: pointer;
        }

        .dropdown-menu {
            border-radius: 12px;
            border: none;
            box-shadow: 0 10px 30px rgba(0,0,0,0.1);
            padding: 10px;
            margin-top: 10px !important;
        }

        .dropdown-item {
            padding: 10px 15px;
            border-radius: 8px;
            font-weight: 500;
            color: #495057;
            display: flex;
            align-items: center;
            transition: 0.2s;
        }

        .dropdown-item i {
            width: 25px;
            font-size: 1.1rem;
            color: #adb5bd;
        }

        .dropdown-item:hover {
            background: #f8fafc;
            color: var(--primary-color);
        }

        .dropdown-item:hover i {
            color: var(--primary-color);
        }

        .content-body { padding: 25px; }

        @media (max-width: 992px) {
            .sidebar { left: -260px; }
            .main-wrapper { margin-left: 0; }
            .sidebar.show { left: 0; }
        }
    </style>
</head>

<body>
    <div class="sidebar" id="sidebar">
        <div class="text-center py-4 mb-2 border-bottom border-secondary">
            <h5 class="fw-bold text-white mb-0">MASTER ADMIN</h5>
        </div>
        <ul class="nav flex-column">
            <li class="nav-item"><a href="/dashboard" class="nav-link"><i class="fa fa-home"></i> Dashboard</a></li>
            <li class="nav-item"><a href="/addAdminPage" class="nav-link"><i class="fa fa-user-plus"></i> Add Admin</a></li>
            <li class="nav-item"><a href="/viewAdminPage" class="nav-link"><i class="fa fa-users"></i> View Admins</a></li>
            
        </ul>
    </div>

    <div class="main-wrapper">
        <nav class="navbar navbar-expand-lg">
            <div class="container-fluid p-0">
                <button class="btn d-md-none me-3" onclick="document.getElementById('sidebar').classList.toggle('show')">
                    <i class="fa fa-bars"></i>
                </button>
                
                <h6 class="mb-0 fw-bold d-none d-md-block">Panel / Dashboard</h6>

                <div class="ms-auto d-flex align-items-center">
                    <div class="text-end me-3 d-none d-sm-block">
                        <p class="mb-0 small fw-bold text-dark"><%= admin.fname %> <%= admin.lname %></p>
                        <p class="mb-0 text-muted" style="font-size: 10px;"><%= admin.email %></p>
                    </div>

                   <div class="dropdown">
    <a href="javascript:void(0)" class="dropdown-toggle d-block" id="userMenu" data-bs-toggle="dropdown" aria-expanded="false" style="text-decoration: none;">
        <img src="/<%= admin.profile_image %>" class="profile-pic" alt="Admin">
    </a>
    <ul class="dropdown-menu dropdown-menu-end shadow" aria-labelledby="userMenu">
        <li>
            <a class="dropdown-item" href="/profile">
                <i class="fa fa-id-badge me-2"></i> My Profile
            </a>
        </li>
        <li>
            <a class="dropdown-item" href="/changepassword">
                <i class="fa fa-key me-2"></i> Password
            </a>
        </li>
        <li><hr class="dropdown-divider"></li>
        <li>
            <a class="dropdown-item text-danger" href="/logout">
                <i class="fa fa-sign-out-alt me-2"></i> Logout
            </a>
        </li>
    </ul>
</div>
                </div>
            </div>
        </nav>
        
        <div class="content-body">

    <script>
        document.addEventListener("DOMContentLoaded", () => {
            const currentPath = window.location.pathname;
            document.querySelectorAll('.sidebar .nav-link').forEach(link => {
                if (link.getAttribute('href') === currentPath) {
                    link.classList.add('active');
                }
            });
        });
    </script>