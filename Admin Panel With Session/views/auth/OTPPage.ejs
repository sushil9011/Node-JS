<!DOCTYPE html>
<html dir="ltr">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Verify OTP | Admin Portal</title>
    
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.6.2/dist/css/bootstrap.min.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/MaterialDesign-Webfont/7.2.96/css/materialdesignicons.min.css">
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600;700&display=swap" rel="stylesheet">
    
    <style>
        body, html { 
            height: 100%; 
            margin: 0; 
            font-family: 'Inter', sans-serif;
            background: linear-gradient(135deg, #0f172a 0%, #334155 100%);
            
            display: flex; 
            align-items: center; 
            justify-content: center; 
        }

        .auth-box { 
            background: #ffffffba; 
            padding: 45px 35px; 
            border-radius: 24px; 
            box-shadow: 0 20px 40px rgba(0,0,0,0.15); 
            width: 100%; 
            max-width: 400px; 
        }

        .profile-img { 
            width: 90px; 
            height: 90px; 
            border-radius: 50%; 
            object-fit: cover; 
            border: 4px solid #f8f9fa; 
            box-shadow: 0 4px 10px rgba(0,0,0,0.1); 
        }

        .otp-input { 
            border-radius: 12px; 
            padding: 15px; 
            height: auto; 
            text-align: center; 
            font-size: 28px; 
            font-weight: 800; 
            letter-spacing: 10px; 
            border: 2px solid #eef2f7; 
            background: #f8fafc;
            color: #11998e;
            transition: all 0.3s;
        }

        .otp-input:focus {
            border-color: #11998e;
            background: #fff;
            box-shadow: 0 0 0 4px rgba(17, 153, 142, 0.1);
            outline: none;
        }

        .btn-verify { 
            background: #11998e; 
            border: none; 
            border-radius: 12px; 
            padding: 14px; 
            font-weight: 700; 
            color: white; 
            width: 100%; 
            transition: 0.3s; 
            font-size: 16px;
        }

        .btn-verify:hover { 
            background: #0d7a71; 
            transform: translateY(-2px); 
            box-shadow: 0 8px 15px rgba(13, 122, 113, 0.3);
        }

        .email-highlight {
            color: #11998e;
            font-weight: 600;
        }

        /* Chrome, Safari, Edge, Opera - Remove arrows */
        input::-webkit-outer-spin-button,
        input::-webkit-inner-spin-button {
            -webkit-appearance: none;
            margin: 0;
        }
    </style>
</head>
<body>
    <div class="auth-box text-center">
        <div class="mb-4">
            <div class="position-relative d-inline-block">
                <img src="/<%= admin.profile_image %>" alt="Admin" class="profile-img mb-3">
            </div>
            <h4 class="font-weight-bold text-dark">Verify Identity</h4>
            <p class="text-muted small">
                Hi <b><%= admin.fname %></b>, please enter the 6-digit code sent to <br> 
                <span class="email-highlight"><%= admin.email %></span>
            </p>
        </div>
        
        <form action="/otp-verify" method="post">
            <div class="form-group mb-4">
                <input type="number" 
                       class="form-control otp-input" 
                       name="adminOTP" 
                       placeholder="000000" 
                       maxlength="6"
                       oninput="if(this.value.length > 6) this.value = this.value.slice(0, 6);" 
                       required 
                       autofocus>
            </div>

            <button type="submit" class="btn btn-verify shadow-sm">
                Verify & Continue
            </button>
            
            <div class="mt-4">
                <a href="/" class="text-muted small text-decoration-none hover-dark">
                    <i class="mdi mdi-arrow-left"></i> Back to Login
                </a>
            </div>
        </form>
    </div>
</body>
</html>

<%- include('../footer.ejs') %>