<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Sushil Admin | Dashboard</title>

    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.1/font/bootstrap-icons.css">
    <link href="https://cdn.jsdelivr.net/npm/sweetalert2@11.26.18/dist/sweetalert2.min.css" rel="stylesheet">

    <style>
        :root {
            --sidebar-width: 260px;
            --dark-navy: #12121c;
            --primary-color: #141417;
            --bg-light: #f4f7f6;
            --neon-blue: #4facfe;
            --neon-cyan: #00f2fe;
        }

        body {
            font-family: 'Inter', sans-serif;
            background-color: var(--bg-light);
            margin: 0;
        }

        /* Sidebar Main */
        .sidebar {
            width: var(--sidebar-width);
            height: 100vh;
            background: var(--dark-navy);
            color: white;
            position: fixed;
            left: 0;
            top: 0;
            transition: 0.3s;
            z-index: 1050;
            overflow-y: auto;
            border-right: 1px solid rgba(255, 255, 255, 0.05);
        }

        /* Main Wrapper */
        .main-wrapper {
            margin-left: var(--sidebar-width);
            min-height: 100vh;
            transition: 0.3s;
            background-color: var(--bg-light);
        }

       /* Navbar (Restored from Old Code) */
        .navbar {
            background: white;
            padding: 12px 30px;
            border-bottom: 1px solid #e9ecef;
        }

        /* Floating Pill Nav Links */
        .nav-link {
            color: #adb5bd !important;
            padding: 10px 20px;
            border-radius: 50px !important;
            margin: 8px 15px;
            transition: all 0.3s ease;
            display: flex;
            align-items: center;
            text-decoration: none;
            background: rgba(255, 255, 255, 0.03);
            border: 1px solid rgba(255, 255, 255, 0.05);
            font-size: 0.9rem;
        }

        /* Hover & Active States */
        .nav-link:hover {
            background: rgba(255, 255, 255, 0.1) !important;
            color: white !important;
            transform: translateX(5px);
        }

        .nav-link.active {
            background: var(--primary-color) !important;
            color: white !important;
            box-shadow: 0 4px 15px rgba(33, 76, 161, 0.3);
            border-color: rgba(255, 255, 255, 0.2);
        }

        /* Profile Dropdown Styling (Restored from Old Code) */
        .profile-pic {
            width: 38px; height: 38px;
            border-radius: 50%;
            object-fit: cover;
            border: 2px solid var(--primary-color);
            cursor: pointer;
        }

        .dropdown-menu {
            border-radius: 12px;
            border: none;
            box-shadow: 0 10px 30px rgba(0,0,0,0.1);
            padding: 10px;
            margin-top: 10px !important;
        }

        .dropdown-item {
            padding: 10px 15px;
            border-radius: 8px;
            font-weight: 500;
            color: #495057;
            display: flex;
            align-items: center;
            transition: 0.2s;
        }

        .dropdown-item i {
            width: 25px;
            font-size: 1.1rem;
            color: #adb5bd;
        }

        .dropdown-item:hover {
            background: #f8fafc;
            color: var(--primary-color);
        }

        /* Dropdown Chevron Animation */
        .transition-icon {
            transition: transform 0.3s ease;
        }

        .nav-link[aria-expanded="true"] .transition-icon {
            transform: rotate(180deg);
        }

        .content-body {
            padding: 25px;
        }

        /* Media Queries */
        @media (max-width: 992px) {
            .sidebar { left: -260px; }
            .main-wrapper { margin-left: 0; }
            .sidebar.show { left: 0; }
        }

        /* Custom Scrollbar */
        .sidebar::-webkit-scrollbar { width: 5px; }
        .sidebar::-webkit-scrollbar-thumb {
            background: rgba(255, 255, 255, 0.1);
            border-radius: 10px;
        }
    </style>
</head>

<body>

    <div class="sidebar" id="sidebar">
        <div class="text-center py-4 mb-3 border-bottom border-secondary border-opacity-25">
            <h5 class="fw-bold text-white mb-0" style="letter-spacing: 1px;">MASTER ADMIN</h5>
        </div>

        <ul class="nav flex-column">
            <li class="nav-item">
                <a href="/dashboard" class="nav-link" id="nav-dashboard">
                    <i class="fa fa-home me-2" style="color: var(--neon-blue);"></i> Dashboard
                </a>
            </li>

            <li class="nav-item">
                <a class="nav-link d-flex justify-content-between align-items-center" data-bs-toggle="collapse"
                    href="#adminManagerMenu" role="button" id="parent-admin-manager">
                    <span>
                        <i class="fa fa-user-shield me-2" style="color: var(--neon-blue);"></i>
                        <span class="fw-bold">Admin Manager</span>
                    </span>
                    <i class="fa fa-chevron-down small opacity-50 transition-icon"></i>
                </a>

                <div class="collapse" id="adminManagerMenu">
                    <ul class="nav flex-column px-3 mt-1">
                        <li class="nav-item mb-1">
                            <a href="/addAdminPage" class="nav-link py-2 shadow-sm" style="font-size: 0.82rem;">
                                <i class="bi bi-plus-circle-fill me-2" style="color: var(--neon-blue);"></i> Add New
                                Entry
                            </a>
                        </li>
                        <li class="nav-item">
                            <a href="/viewAdminPage" class="nav-link py-2 shadow-sm" style="font-size: 0.82rem;">
                                <i class="bi bi-eye-fill me-2" style="color: var(--neon-cyan);"></i> Manage Admins
                            </a>
                        </li>
                    </ul>
                </div>
            </li>
        </ul>
    </div>
    <div class="main-wrapper">

        <nav class="navbar navbar-expand-lg">
            <div class="container-fluid p-0">
                <button class="btn d-md-none me-3" onclick="document.getElementById('sidebar').classList.toggle('show')">
                    <i class="fa fa-bars"></i>
                </button>
                
                <h6 class="mb-0 fw-bold d-none d-md-block">Panel / Dashboard</h6>

                <div class="ms-auto d-flex align-items-center">
                    <div class="text-end me-3 d-none d-sm-block">
                        <p class="mb-0 small fw-bold text-dark"><%= admin.fname %> <%= admin.lname %></p>
                        <p class="mb-0 text-muted" style="font-size: 10px;"><%= admin.email %></p>
                    </div>

                    <div class="dropdown">
                        <a href="javascript:void(0)" class="dropdown-toggle d-block" id="userMenu" data-bs-toggle="dropdown" aria-expanded="false" style="text-decoration: none;">
                            <img src="/<%= admin.profile_image %>" class="profile-pic" alt="Admin">
                        </a>
                        <ul class="dropdown-menu dropdown-menu-end shadow" aria-labelledby="userMenu">  
                            <li>
                                <a class="dropdown-item" href="/profile">
                                    <i class="fa fa-id-badge me-2"></i> My Profile
                                </a>
                            </li>
                            <li>
                                <a class="dropdown-item" href="/change-password">
                                    <i class="fa fa-key me-2"></i> Password
                                </a>
                            </li>
                            <li><hr class="dropdown-divider"></li>
                            <li>
                                <a class="dropdown-item text-danger" href="/logout">
                                    <i class="fa fa-sign-out-alt me-2"></i> Logout
                                </a>
                            </li>
                        </ul>
                    </div>
                </div>
            </div>
        </nav>

        <div class="content-body">

         